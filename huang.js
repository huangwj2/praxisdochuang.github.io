function copy(text) {
  // var text = document.getElementById("idOfText");
  // text.select();
  // text.setSelectionRange(0, 99999);
  // navigator.clipboard.writeText(text.value);
  navigator.clipboard.writeText(text);
}
